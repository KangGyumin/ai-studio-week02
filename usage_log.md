# AI(LLM) 활용 기록 (Usage Log)

**1. 사용 도구 및 활용 목적**
* **사용 도구:** Gemini
* **주요 활용 목적:** 파이썬 객체지향(OOP) 문법 질의응답 및 Git 터미널 에러 디버깅

**2. 주요 질문 및 채택/수정 내용**
* **Q: 파이썬 클래스에서 `init` 대신 `__init__`을 쓰고 `self`를 넣는 이유는 무엇인가?**
  * **답변 요지:** `__init__`은 객체 생성 시 자동 실행되는 매직 메서드이며, `self`는 호출한 인스턴스 자신을 가리키는 역할을 함을 비유를 통해 설명받음.
  * **반영 내용:** 파이썬의 객체지향 동작 원리를 이해하고, Customer 및 Order 클래스 뼈대를 직접 구현할 때 해당 문법을 정확히 반영하여 작성함.
  
* **Q: Order 클래스에서 `items if items is not None else []` 구문을 쓰는 이유와 튜플 언패킹 방식 질문**
  * **답변 요지:** 파라미터 기본값으로 `[]`를 쓰면 모든 객체가 하나의 리스트를 공유하는 참조 버그가 발생하며, 반복문에서 쓰지 않는 변수는 `_`로 무시하고 필요한 값만 꺼내는 방식을 설명받음.
  * **반영 내용:** 변수명 충돌을 방지하고 튜플 언패킹(`for _, price`)을 활용하여 안전하게 `total_price()` 로직을 디버깅하고 완성함.

* **Q: 터미널에서 커밋 메시지 작성 중 따옴표 오타로 인해 `>>` 대기 상태에 빠졌을 때 해결 방법**
  * **답변 요지:** `Ctrl + C`로 빠져나온 후 올바른 따옴표로 다시 커밋을 진행하라는 가이드를 받음.
  * **반영 내용:** 명령어 꼬임 상태를 해결한 뒤, 의도적 충돌(Merge Conflict)을 정상적으로 발생시키고 병합 및 해결을 완료함.